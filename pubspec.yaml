name: sicopa
description: Sistema de Control Patrimonial (SICOPA) del Estado de México.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.1.0
  cloud_firestore: ^5.0.2
  firebase_auth: ^5.1.1
  cloud_functions: ^5.0.1
  mobile_scanner: 6.0.11
  nfc_manager: ^3.3.0
  file_picker: ^8.1.0
  csv: ^6.0.0
  pdf: ^3.11.1
  printing: ^5.13.2
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
